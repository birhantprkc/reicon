# Cloudflare Pages Headers Configuration

# Rule 1: All HTML routes and pages — never cache so deployment updates are instantly live
/*
  Cache-Control: public, max-age=0, must-revalidate

# Rule 2: Immutable caching for hashed JS/CSS assets
/assets/*
  Cache-Control: public, max-age=31536000, immutable
